# Supplied Spline Reference Notes

The two supplied URLs lead to Spline editor pages titled `Poster 2 - Copy - Copy` and `Projection Sphere - Copy - Copy`.

Both addresses are editor or preview routes rather than direct public embed URLs. The first loaded as an opaque dark canvas with no inspectable scene controls. The second showed an unnamed Spline workspace with a login prompt and did not expose a scene to inspect or embed. Neither should be embedded into a public portfolio build in its current form.

The appropriate integration route is a published Spline scene URL ending in `.splinecode` or a Spline share link with public viewing enabled. Once one is available, the fictional flagship can use it for a single scene such as a garment form, reflective logo sculpture, or folded fabric object. Until then, the existing purpose-built WebGL material study is more honest, controllable, and performance-safe.

