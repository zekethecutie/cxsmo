import { ArrowLeft, ArrowRight, CheckCircle2, MapPin, ShieldCheck, Truck } from "lucide-react";
import { useCallback, useState } from "react";
import { Link } from "wouter";
import { MapView } from "@/components/Map";
import { useCxsmoDemo } from "@/contexts/CxsmoDemoContext";
import { cxsmoProducts, formatCxsmoPrice } from "@/lib/cxsmo";
import "@/pages/cxsmo-checkout.css";
import "@/pages/cxsmo-checkout-flow.css";

const steps = ["Review", "Address", "Delivery", "Confirm"] as const;

export function CxsmoCheckoutSimulation() {
  const { bag, profile } = useCxsmoDemo();
  const [step, setStep] = useState(0);
  const [destination, setDestination] = useState(profile.destination);
  const [showMap, setShowMap] = useState(false);
  const [delivery, setDelivery] = useState("Standard presentation");
  const [acknowledged, setAcknowledged] = useState(false);
  const [staged, setStaged] = useState(false);
  const lines = bag.map((line) => ({ ...line, product: cxsmoProducts.find((item) => item.id === line.productId) })).filter((line): line is typeof line & { product: NonNullable<typeof line.product> } => Boolean(line.product));
  const total = lines.reduce((sum, line) => sum + line.product.price, 0);
  const onMapReady = useCallback((map: google.maps.Map) => { if (!destination.trim() || !window.google?.maps?.Geocoder) return; const geocoder = new window.google.maps.Geocoder(); geocoder.geocode({ address: destination }, (results, status) => { if (status !== "OK" || !results?.[0]) return; const location = results[0].geometry.location; map.setCenter(location); map.setZoom(13); new window.google.maps.Marker({ map, position: location, title: "Browser-only delivery preview" }); }); }, [destination]);
  const next = () => setStep((current) => Math.min(current + 1, steps.length - 1));
  const previous = () => setStep((current) => Math.max(current - 1, 0));
  if (!lines.length) return <section className="cxsmo-checkout"><p className="section-label">Checkout / simulation</p><h1>Nothing to stage<br /><em>yet.</em></h1><p>Add a fictional C✦SMO object to preview a portfolio checkout.</p><Link className="cxsmo-button" href="/cxsmo/shop">Browse the drop</Link></section>;
  return <section className="cxsmo-checkout"><div className="cxsmo-checkout__head"><div><p className="section-label">Checkout / browser simulation</p><h1>One last<br /><em>signal.</em></h1></div><Link href="/cxsmo/bag"><ArrowLeft size={15} /> Back to bag</Link></div><div className="cxsmo-checkout__notice"><ShieldCheck size={20} /><p><b>Display-only checkout.</b> No payment, order, address, or contact information is transmitted or retained by C✦SMO. The map is a browser-session delivery preview only.</p></div><ol className="cxsmo-checkout-steps" aria-label="Checkout progress">{steps.map((label, index) => <li key={label} className={index === step ? "is-active" : index < step ? "is-done" : ""}><span>0{index + 1}</span>{label}</li>)}</ol><div className="cxsmo-checkout__grid"><div className="cxsmo-checkout-flow" aria-live="polite">{step === 0 && <section><p className="section-label">01 / Bag review</p><h2>Your selected<br /><em>objects.</em></h2><p>Review the fictional objects and sizes before entering the browser-only address presentation.</p></section>}{step === 1 && <section><p className="section-label">02 / Address preview</p><h2>Find the<br /><em>signal.</em></h2><label>Destination for map preview<input value={destination} onChange={(event) => { setDestination(event.target.value); setShowMap(false); }} placeholder="City and country" /></label><button type="button" className="cxsmo-checkout__map-button" onClick={() => setShowMap(Boolean(destination.trim()))}><MapPin size={16} /> Preview location on map</button>{showMap && <div className="cxsmo-checkout__map"><MapView key={destination} initialZoom={3} className="h-[280px]" onMapReady={onMapReady} /></div>}<small>This field remains in the current browser session. It is not used to create a delivery address.</small></section>}{step === 2 && <section><p className="section-label">03 / Delivery presentation</p><h2>Choose a<br /><em>rhythm.</em></h2><p>These controls demonstrate delivery-information hierarchy only. They do not provide service availability, parcel timing, or a fulfilment promise.</p><div className="cxsmo-checkout-delivery">{["Standard presentation", "Express presentation"].map((option) => <button type="button" key={option} className={delivery === option ? "is-active" : ""} onClick={() => setDelivery(option)}><Truck size={17} /><span><b>{option}</b><small>Portfolio display state only</small></span></button>)}</div></section>}{step === 3 && <section><p className="section-label">04 / Confirmation</p><h2>Preview, not<br /><em>purchase.</em></h2><p>Review the staged display below. A real checkout would require a verified store, merchant, policies, payment provider, and authenticated order record.</p><label className="cxsmo-checkout__consent"><input type="checkbox" checked={acknowledged} onChange={(event) => setAcknowledged(event.target.checked)} /><span>I understand this is a fictional checkout demonstration and that no purchase, delivery, or personal-data record will be created.</span></label>{staged && <p className="cxsmo-checkout__success"><CheckCircle2 size={17} /> Preview staged in this browser only. No order reference, receipt, payment, or personal-data record has been created.</p>}</section>}<div className="cxsmo-checkout-flow__actions">{step > 0 && <button type="button" className="cxsmo-text-button" onClick={previous}><ArrowLeft size={15} /> Back</button>}{step < steps.length - 1 ? <button type="button" className="cxsmo-button" onClick={next} disabled={step === 1 && !destination.trim()}>Continue <ArrowRight size={16} /></button> : <button className="cxsmo-button" type="button" disabled={!acknowledged} onClick={() => setStaged(true)}>{staged ? <><CheckCircle2 size={16} /> Preview staged</> : "Stage checkout preview"}</button>}</div></div><aside><p>Order simulation</p>{lines.map((line, index) => <div key={`${line.productId}-${index}`}><img src={line.product.image} alt="" /><span><b>{line.product.name}</b><small>Size {line.size}</small></span><em>{formatCxsmoPrice(line.product.price, profile.locale, profile.currency, profile.currencyRate)}</em></div>)}<div className="cxsmo-checkout__total"><span>Portfolio total</span><b>{formatCxsmoPrice(total, profile.locale, profile.currency, profile.currencyRate)}</b></div><small>{delivery}. Currency display follows your local C✦SMO preference; prices and exchange conversion are fictional presentation values.</small></aside></div></section>;
}
